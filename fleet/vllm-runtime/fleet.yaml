defaultNamespace: inference-system

# This is now a Helm chart
# Override values.yaml to deploy different models
helm:
  releaseName: vllm-opt-125m
  chart: .
  # Uncomment to customize:
  # valuesFiles:
  #   - values-llama3.yaml
