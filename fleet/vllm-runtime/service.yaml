apiVersion: v1
kind: Service
metadata:
  name: vllm-runtime
  namespace: inference-system
  labels:
    app: vllm-runtime
spec:
  type: ClusterIP
  selector:
    app: vllm-runtime
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
